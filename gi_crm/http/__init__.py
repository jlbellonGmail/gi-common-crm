"""Capa HTTP opcional (extra `gi-common-crm[http]`). No se importa desde
gi_crm.__init__ ni desde ningún módulo de la biblioteca base: quien sólo
consume `gi_crm.api`/`gi_crm.service` no necesita FastAPI instalado.
"""
